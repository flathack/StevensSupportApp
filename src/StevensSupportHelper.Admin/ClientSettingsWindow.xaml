<Window x:Class="StevensSupportHelper.Admin.ClientSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Client Settings"
        Height="560"
        Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Text="Client Metadata" />

        <TextBlock Grid.Row="1"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="RustDesk ID" />
        <TextBox x:Name="RustDeskIdTextBox"
                 Grid.Row="2"
                 Height="34"
                 VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="3"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="RustDesk Password" />
        <PasswordBox x:Name="RustDeskPasswordBox"
                     Grid.Row="4"
                     Height="34"
                     VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="5"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="Remote Username Override" />
        <TextBox x:Name="RemoteUserNameTextBox"
                 Grid.Row="6"
                 Height="34"
                 VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="7"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="Remote Password Override" />
        <PasswordBox x:Name="RemotePasswordBox"
                     Grid.Row="8"
                     Height="34"
                     VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="9"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="Notes" />
        <TextBox x:Name="NotesTextBox"
                 Grid.Row="10"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="11"
                    Margin="0,18,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="110"
                    Margin="0,0,10,0"
                    Click="CancelButton_OnClick"
                    Content="Cancel" />
            <Button Width="110"
                    Click="SaveButton_OnClick"
                    Content="Save" />
        </StackPanel>
    </Grid>
</Window>
