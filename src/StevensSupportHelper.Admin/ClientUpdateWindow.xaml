<Window x:Class="StevensSupportHelper.Admin.ClientUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Client"
        Height="760"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Text="Client Update" />

        <Border Grid.Row="1"
                Margin="0,14,0,0"
                Padding="12"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel>
                <TextBlock FontWeight="SemiBold"
                           Text="Connectivity / WinRM" />
                <TextBlock x:Name="TargetHostTextBlock"
                           Margin="0,8,0,0"
                           Text="Target: -" />
                <TextBlock x:Name="CredentialTextBlock"
                           Margin="0,4,0,0"
                           Text="Credentials: -" />
                <TextBlock x:Name="ReachabilityTextBlock"
                           Margin="0,4,0,0"
                           FontWeight="SemiBold"
                           Text="WinRM: checking..." />
                <TextBlock x:Name="PrecheckMessageTextBlock"
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Text="Precheck is running..." />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Text="Installer Path" />

        <TextBox x:Name="InstallerPathTextBox"
                 Grid.Row="3"
                 Height="34"
                 IsReadOnly="True"
                 VerticalContentAlignment="Center" />

        <Border Grid.Row="4"
                Margin="0,14,0,0"
                Padding="12"
                Background="{DynamicResource SurfaceBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel>
                <TextBlock FontWeight="SemiBold"
                           Text="Loaded client.installer.config" />
                <TextBlock x:Name="ConfigSourceTextBlock"
                           Margin="0,6,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           TextWrapping="Wrap"
                           Text="Loading current client configuration..." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="5"
              Margin="0,14,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock FontWeight="SemiBold"
                       Text="client.installer.config Content" />

            <TextBox x:Name="ConfigTextBox"
                     Grid.Row="1"
                     Margin="0,8,0,0"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" />
        </Grid>

        <DockPanel Grid.Row="6"
                   Margin="0,18,0,0"
                   LastChildFill="False">
            <TextBlock x:Name="StatusTextBlock"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       Text="Ready" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right">
                <Button Width="110"
                        Margin="0,0,10,0"
                        Click="CancelButton_OnClick"
                        Content="Cancel" />
                <Button x:Name="RunUpdateButton"
                        Width="170"
                        Click="RunUpdateButton_OnClick"
                        Content="Update durchfuehren" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
